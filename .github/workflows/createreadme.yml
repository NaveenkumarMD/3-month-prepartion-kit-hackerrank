name: Generating readme file
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup python
        uses: actions/setup-python@v2
        with:
          python-version: "3.8"
      - name : Running Writereadme.py file
        run: python Writereadme.py
  Refactor_Folder_Structure:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{secrets.ACCESS_TOKEN}}
      - run: |
          git config user.name "Naveenkumar M"
          git config user.email naveen9715568487@gmail.com
          git add .
          git commit -m "generating reafctored files"
          git push origin master

          






          